<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.noos.backend.eeg.mapper.EegMapper">

    <insert id="insertEegSession" useGeneratedKeys="true" keyProperty="eegSessionId">
        INSERT INTO eeg_sessions
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            device_type,
            status,
            measured_at,
            created_at
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            #{deviceType},
            #{status},
            <choose>
                <when test="measuredAt != null">#{measuredAt},</when>
                <otherwise>NOW(),</otherwise>
            </choose>
            NOW()
        </trim>
    </insert>

    <select id="selectEegSessionById" resultType="com.noos.backend.eeg.dto.EegSession">
        SELECT
            eeg_session_id AS eegSessionId,
            user_id AS userId,
            device_type AS deviceType,
            status,
            measured_at AS measuredAt,
            created_at AS createdAt
        FROM eeg_sessions
        WHERE eeg_session_id = #{eegSessionId}
    </select>

    <update id="updateEegSessionStatus">
        UPDATE eeg_sessions
        SET status = #{status}
        WHERE eeg_session_id = #{eegSessionId}
    </update>

    <insert id="insertEegResult" useGeneratedKeys="true" keyProperty="eegResultId">
        INSERT INTO eeg_results (
            eeg_session_id,
            delta,
            theta,
            alpha,
            beta,
            gamma,
            dominant_band,
            state_key,
            state_label,
            confidence,
            quality_score,
            feature_source,
            focus_score,
            relax_score,
            stress_score,
            mental_workload_score,
            fatigue_risk_score,
            cortical_arousal_score,
            analysis_version,
            raw_ai_response_json,
            created_at
        ) VALUES (
            #{eegSessionId},
            #{delta},
            #{theta},
            #{alpha},
            #{beta},
            #{gamma},
            #{dominantBand},
            #{stateKey},
            #{stateLabel},
            #{confidence},
            #{qualityScore},
            #{featureSource},
            #{focusScore},
            #{relaxScore},
            #{stressScore},
            #{mentalWorkloadScore},
            #{fatigueRiskScore},
            #{corticalArousalScore},
            #{analysisVersion},
            #{rawAiResponseJson},
            NOW()
        )
    </insert>

    <insert id="insertEegWindowResult" useGeneratedKeys="true" keyProperty="eegWindowResultId">
        INSERT INTO eeg_window_results (
            eeg_session_id,
            window_index,
            window_start_at,
            window_end_at,
            window_duration_sec,
            sample_count,
            sample_rate_hz,
            analysis_mode,
            delta,
            theta,
            alpha,
            beta,
            gamma,
            dominant_band,
            state_key,
            state_label,
            confidence,
            quality_score,
            feature_source,
            focus_score,
            relax_score,
            stress_score,
            mental_workload_score,
            fatigue_risk_score,
            cortical_arousal_score,
            analysis_version,
            raw_ai_response_json,
            created_at
        ) VALUES (
            #{eegSessionId},
            #{windowIndex},
            #{windowStartAt},
            #{windowEndAt},
            #{windowDurationSec},
            #{sampleCount},
            #{sampleRateHz},
            #{analysisMode},
            #{delta},
            #{theta},
            #{alpha},
            #{beta},
            #{gamma},
            #{dominantBand},
            #{stateKey},
            #{stateLabel},
            #{confidence},
            #{qualityScore},
            #{featureSource},
            #{focusScore},
            #{relaxScore},
            #{stressScore},
            #{mentalWorkloadScore},
            #{fatigueRiskScore},
            #{corticalArousalScore},
            #{analysisVersion},
            #{rawAiResponseJson},
            NOW()
        )
    </insert>

</mapper>
