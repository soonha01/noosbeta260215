<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.noos.backend.eeg.mapper.EegMapper">

    <insert id="insertEegSession" useGeneratedKeys="true" keyProperty="eegSessionId">
        INSERT INTO eeg_sessions
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            device_type,
            status,
            measured_at,
            created_at
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            #{deviceType},
            #{status},
            <choose>
                <when test="measuredAt != null">#{measuredAt},</when>
                <otherwise>NOW(),</otherwise>
            </choose>
            NOW()
        </trim>
    </insert>

    <update id="updateEegSessionStatus">
        UPDATE eeg_sessions
        SET status = #{status}
        WHERE eeg_session_id = #{eegSessionId}
    </update>

    <insert id="insertEegResult" useGeneratedKeys="true" keyProperty="eegResultId">
        INSERT INTO eeg_results (
            eeg_session_id,
            delta,
            theta,
            alpha,
            beta,
            gamma,
            dominant_band,
            state_label,
            confidence,
            focus_score,
            relax_score,
            stress_score,
            created_at
        ) VALUES (
            #{eegSessionId},
            #{delta},
            #{theta},
            #{alpha},
            #{beta},
            #{gamma},
            #{dominantBand},
            #{stateLabel},
            #{confidence},
            #{focusScore},
            #{relaxScore},
            #{stressScore},
            NOW()
        )
    </insert>

</mapper>
